# Keeps the SHA-pinned workflow actions up to date.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels:
      - needs-triage
    groups:
      # One combined PR per week instead of one PR per action.
      actions:
        patterns:
          - "*"
