package dev.ishaankot.worldarchive.ui.model;

/** Supported deterministic backup-browser orderings. */
public enum BackupSort {
    NEWEST,
    OLDEST,
    LABEL,
    SIZE_DESCENDING,
    CHANGED_FILES_DESCENDING
}
