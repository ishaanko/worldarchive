package dev.ishaankot.worldarchive.settings;

/** Stable identifiers for settings validation and user-interface feedback. */
public enum SettingsField {
    DESTINATIONS,
    SCHEDULE_INTERVAL,
    GIT_REPOSITORY,
    GIT_REMOTE_NAME,
    GIT_REMOTE_URL,
    GIT_LFS_PATTERNS,
    ZIP_DESTINATION
}
